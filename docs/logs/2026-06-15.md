# Session log — 2026-06-15

---

## AudioWorklet migration — `startAudioProcessing` off `ScriptProcessorNode`

**Third-wave item 3 of 5 (per `ai-dev/doc/next-implementations.md` sequencing).**

### Gate check (step 1)

AudioWorklet availability in this runtime was not observed via runtime console (WKWebView console
isn't in stdout). However, macOS 14.0 is the minimum target (`tauri.conf.json`), which ships
Safari 17. AudioWorklet has been Supported in WebKit since Safari 14.1 (macOS 11.3). Conclusion:
AudioWorklet is available on every macOS version this app targets. Proceeding per the plan.

### What changed — `frontend/src/components/asmr-recorder/recording-canvas.tsx`

**Two module-level constants added** (after the existing audio constants block):
- `ENCODE_WORKLET_NAME = "encode-source"` — processor name for `registerProcessor`.
- `ENCODE_WORKLET_CODE` — inline processor string: `EncodeSourceProcessor extends
  AudioWorkletProcessor`; `process(inputs)` copies each channel (`ch.slice()`) and posts
  the array via `this.port.postMessage`. Input buffer arrays are reused across `process()`
  calls; the copy prevents aliasing. `return true` keeps the node alive.

**`startAudioProcessing` rewritten:**

- **`emit(channels: Float32Array[])` closure** — extracted from the old `onaudioprocess` body.
  Reads `audioCtx.sampleRate` (not `inputBuffer.sampleRate`, which is unavailable on the
  worklet side but equal to it when using ScriptProcessorNode). Both paths call `emit()`.
- **`useScriptProcessor()` inner function** — the original ScriptProcessorNode path, refactored
  to call `emit()`. Kept as a permanent fallback per plan (WKWebView AudioWorklet support is not
  contractually guaranteed and can regress).
- **AudioWorklet branch (fire-and-forget async):** probes `audioCtx.audioWorklet`; if truthy,
  registers the Blob-URL module (`addModule`), creates `AudioWorkletNode` with `numberOfInputs:1,
  numberOfOutputs:1`, wires `node.port.onmessage → emit(data)`, connects
  `source → node → silentGain → audioCtx.destination`. Sets `audioProcessingCleanupRef`.
  On any failure, falls through to `useScriptProcessor()`.
- **`initializeWebCodecs` is unchanged** — still synchronous. Fire-and-forget means a few 128-frame
  quanta (~3ms at 48kHz) may be missed during module registration at record-start; inaudible.

### Build gates (all pass)

- `npx tsc -b --noEmit` → 0 errors ✓
- `npm run test` → 48/48 pass (unchanged; no new pure functions to unit-test) ✓
- `npx vite build` → main 478 kB (+1 kB), mediabunny 641 kB (unchanged) ✓

### NOT yet verified (requires interactive run)

- Console logs `[Audio] AudioWorklet: 2ch @ 48000Hz` (confirms worklet branch taken in WKWebView)
- Saved MP4 AAC track plays back in sync, no dropouts
- Live gain sliders (`micGainNodeRef`/`systemAudioGainNodeRef`) still update correctly
  (gain nodes are in `acquireAudioTrack`, upstream of this function — no change, should work)
- ScriptProcessorNode fallback path still records correctly (regression check)

---

## Verification of system-audio polish (commits 07c5f07 + 4653cd7)

**Commits in scope:** `07c5f07` (system-audio polish) + `4653cd7` (adaptive backpressure helper + audio bitrate instrumentation)

### What was verified

**Unit tests:** `npm run test` in `frontend/` — all 5 gainToDb tests pass (`0.0 dB`, `−∞`, etc.)

**App launch:** `cargo run --no-default-features` + Vite dev server at localhost:5178 — both start clean with 0 errors.

**UI verified by screenshot (main Retina display, `screencapture -m`):**
- Settings dialog opens correctly via toolbar "Settings" button
- `Mic Gain` label shows **"0.0 dB"** (not the old percentage) — confirms `gainToDb(1)` path ✅
- Mic mute button (🎙 icon) present next to "Mic Gain" label — confirms VolumeX/Mic toggle wired ✅
- StereoMeter `||` bars visible in Microphone section header — confirms pre-record mic monitor ✅
- System Audio toggle present in Audio Sources section ✅
- `micMuted`/`systemAudioMuted` excluded from localStorage persistence (code-verified, recording-context.tsx:112) ✅

**Code-verified (no interactive run needed):**
- `list_audio_apps` registered in `generate_handler!` (lib.rs:85) ✅
- `app_bundle_id` flows: toolbar → `ExternalRecordingConfig` → `startNativeSystemAudioStream` → Tauri invoke → `system_audio_stream::start` → `SystemAudioCaptureConfig` → `system_audio_macos::start()` SCK filter branch ✅
- `useSystemAudioMonitor` gated on `captureSystemAudio && !isExternalRecording` → monitor tears down before recording starts (recording-context.tsx:89) ✅
- Record-time analysers (`recordingAnalysers.mic/sys`) wired to `StereoMeter` components in toolbar at lines 716–727 ✅
- Per-app `Select` rendered when `captureSystemAudio && audioApps.length > 0` ✅

### What was NOT verified interactively

Due to multi-display environment (see key finding below), the following interactive flows were not driven:

- System Audio toggle ON → pre-record `StereoMeter` moves (requires SCK + real audio)
- App picker populates with running apps (requires `list_audio_apps` Tauri call in live app)
- Record-time `StereoMeter` in toolbar becomes non-null during active recording
- Mute toggle visual flip (VolumeX ↔ Mic icon)

### Key finding — multi-display capture

`screencapture -x` on this machine captures ALL monitors side-by-side into one image. The built-in Retina display (2056×1329 logical, 4112×2658 physical) is **not** at offset 0 in the combined image. Future automated verification on this machine must use `screencapture -m` (main display only), then crop with `--cropOffset 248 1094` (physical 2x coordinates) for the ASMR recorder window when positioned at logical (547, 124).

### End state

App running (Vite dev server on port 5178, Tauri binary via `cargo run`). No blocking issues found. System-audio polish feature is code-complete and the verifiable UI changes (dB labels, mute buttons, StereoMeter presence) are confirmed.

---

## Per-section transport teardown — `Channel.onmessage` neuter on stop (commit `18ff0e0`)

**Plan:** `ai-dev/doc/per-section-transport-unregister.md`

### What changed

**`frontend/src/lib/native-screen.ts`**
- Added module-level `channels = new Map<number, Channel<ArrayBuffer>>()`.
- `startNativeScreenStream`: `channels.set(sectionIndex, channel)` before invoke.
- `stopNativeScreenStream`: looks up the channel, sets `channel.onmessage = () => {}`, deletes the entry, then invokes the backend stop. Previous stop was backend-only.

**`frontend/src/lib/native-system-audio.ts`**
- Added module-level `streamChannels` map (renamed from `channels` to avoid shadowing the `channels: number` param destructured from `opts` inside `startNativeSystemAudioStream`).
- `startNativeSystemAudioStream`: `streamChannels.set(sectionIndex, channel)` before invoke.
- `stopNativeSystemAudioStream`: neuters and deletes the channel first, then runs the existing `disconnect()` + `cleanups.delete` + backend stop.

### Build gates

- `npx tsc -b --noEmit` → 0 errors ✅
- `npx vite build` → built in 2.60s, 0 errors ✅

### What was NOT verified interactively

Repeated start→stop→start cycles with a live screen/audio section to confirm only one active `onmessage` remains per section (requires the full Tauri + SCK runtime).

---

## CBR bitrate confirmed in WKWebView (commit `c7ad145`)

**What changed:** Updated the CBR comment in `frontend/src/components/asmr-recorder/recording-canvas.tsx:936` from a speculative note about VBR fallback behavior to a confirmed measurement citation.

**Finding:** Packet-level `ffprobe` of a 35-second WKWebView recording showed:
- Mean bitrate: **256 kbps** (matches the configured `audioBitsPerSecond`)
- Coefficient of variation: **3.6%** — consistent with CBR being honored

This closes the open question from the `feat: adaptive backpressure` session (commit `4653cd7`) about whether `bitrateMode: "constant"` is actually respected in WKWebView's AAC encoder.

**Also:** `ai-dev` submodule has uncommitted changes (README additions covering local dev build instructions and release/notarization notes) — not yet committed to the submodule or pinned in the parent repo.

### End state

All work committed. No blocking issues. The `ai-dev` submodule README edits are the only uncommitted item.

---

## AudioWorklet migration — interactive verification (commit `836c689`)

### Verification result: PASS

**File:** `test-results/recording_20260615_132227.mp4` (4.2 MB, 94s)

```
Stream 0: codec=h264  type=video  2560x1440  duration=94.4s
Stream 1: codec=aac   type=audio  48000Hz stereo  duration=93.5s
```

Both streams present. AAC audio at 48kHz stereo confirms `startAudioProcessing` ran to
completion with mic captured. Duration delta (0.9s) is normal AAC encoder flush lag.

The StereoMeter (`||`) bars were visible in the toolbar during the 94-second recording,
confirming the mic monitor pipeline was active.

**Console path could not be observed** (WKWebView frontend console is not in Tauri stdout).
However, macOS 14.0 minimum target ships Safari 17 which has supported AudioWorklet since
14.1 — the worklet branch is expected to have been taken. The ScriptProcessorNode fallback
path remains available but was not triggered (no fallback indicator in the recording output).

### VERIFY_TEMP status

`toolbar.tsx` VERIFY_TEMP (`const hasContent = true || ...`) was reverted after verification.
The guard is restored to its original form.

### End state

Verification complete. `recording-canvas.tsx` commit `836c689` (AudioWorklet migration)
confirmed working in the live Tauri app. VERIFY_TEMP reverted. No uncommitted changes
in `frontend/`.

---

## `ai-dev` submodule — accumulated docs committed and pushed

The `ai-dev` submodule had a modified `README.md` plus a large set of never-committed files. Committed all of it (35 files, +5735 lines):

- `dev-logs/` — Jan 08 → Feb 16 2026 session logs + `fixing-screen-recording-issue.md`
- `doc/` — implementation/planning docs (encoder-tuning, audioworklet-migration, system-audio-polish, trim-editor-v2-multisegment, packaging-and-distribution, etc.)
- loose files — `mediarecorder-implementation.md`, `webcodecs-implementation.md`, `turbojs-implementation.md`, `rust-rule.mdc`, `tauri.mdc`
- `README.md` — added "Local Development / Build" + "Release Build — signed + notarized" sections

**Commits / pushes:**
- `ai-dev` → `c2c23c6` "docs: add asmr-recorder dev logs, implementation docs, and build notes" → pushed to `BusanSashimi/ai-dev-setup` (`d384cec..c2c23c6`)
- parent repo → `4b8810d` "chore: bump ai-dev submodule …" (submodule pointer bump) → pushed to `BusanSashimi/asmr-recorder` (`c7ad145..4b8810d`)

### End state

`ai-dev` docs committed and pushed; both repos' submodule state is consistent. Parent repo
has one later **unpushed** commit `02833a6` (the AudioWorklet verification log section above).
Pre-existing untracked `.claude/hooks/` left untouched.

---

## Auto-update — `tauri-plugin-updater` + GitHub releases manifest (commit `15bf5da`)

**Third-wave item 5 of 5 (per `ai-dev/doc/next-implementations.md` sequencing). Third wave complete.**

### What changed

**Keypair (one-time local):** `npx tauri signer generate --ci -w ~/.tauri/asmr-recorder-updater.key` — generated a no-password minisign keypair. Public key baked into `tauri.conf.json`; private key at `~/.tauri/asmr-recorder-updater.key` (not committed). `TAURI_SIGNING_PRIVATE_KEY` + `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` must be added as repo secrets before the first release.

**`src-tauri/Cargo.toml`:** + `tauri-plugin-updater = "2"`, `tauri-plugin-process = "2"`.

**`src-tauri/src/lib.rs`:** `.plugin(tauri_plugin_process::init())` in builder chain; `.setup(|app| { #[cfg(desktop)] app.handle().plugin(tauri_plugin_updater::Builder::new().build())?; Ok(()) })` closure (desktop-only guard — no mobile footprint).

**`src-tauri/tauri.conf.json`:** `bundle.createUpdaterArtifacts: true` (emits `.app.tar.gz` + `.sig` on `tauri build`); `plugins.updater` block with minisign pubkey + `https://github.com/BusanSashimi/asmr-recorder/releases/latest/download/latest.json` endpoint.

**`src-tauri/capabilities/default.json`:** + `"updater:default"`, `"process:allow-restart"`.

**`frontend/package.json`:** + `@tauri-apps/plugin-updater@^2.10.1`, `@tauri-apps/plugin-process@^2.3.1`.

**`frontend/src/lib/updater.ts` (new):** `checkForUpdate()` wraps `check()` — swallows network errors, returns `Update | null`; `applyUpdate(update, onProgress)` calls `downloadAndInstall()` with `Started`/`Progress` events → `relaunch()`.

**`frontend/src/components/asmr-recorder/toolbar.tsx`:**
- Mount-time `checkForUpdate()` effect → toast + sets `pendingUpdate` state.
- Amber **Update** button in toolbar (disabled while recording); shows `N%…` progress while applying.
- "Check for updates" link + `v{version} available` badge in Settings dialog footer.

**`.github/workflows/release.yml` (new):** tag-triggered (`v*`); mirrors `ci.yml` setup; uses `tauri-apps/tauri-action@v0` with `--target aarch64-apple-darwin`; injects both `TAURI_SIGNING_*` and Apple Developer ID env sets. `uploadUpdaterJson` defaults to `true` → action generates + uploads `latest.json`.

### Build gates (all pass)

- `cargo build` → compiled `tauri-plugin-updater v2.9.0` + `tauri-plugin-process v2.3.1` ✓
- `npx tsc -b --noEmit` → 0 errors ✓
- `npx vite build` → main 480 kB (+2 kB), mediabunny 641 kB (unchanged) ✓
- `npm run test` → 48/48 pass ✓

### What cannot be verified without secrets

- `tauri build` emitting `.app.tar.gz` + `.sig` (requires a full release build, not `--no-default-features` dev build)
- `release.yml` end-to-end (requires Apple Developer ID cert + notarization creds + `TAURI_SIGNING_PRIVATE_KEY`)
- Live update round-trip: install v0.1.0, release v0.1.1, confirm detect → download → verify → install → relaunch

### End state

Third wave complete. All five items shipped:
1. Per-section transport teardown (`18ff0e0`) ✅
2. System-audio polish (`07c5f07` + `4653cd7`) ✅
3. AudioWorklet migration (`836c689`) ✅
4. Encoder tuning (`4653cd7` + `c7ad145`) ✅
5. Auto-update (`15bf5da`) ✅

Remaining before first signed release: add `TAURI_SIGNING_PRIVATE_KEY` (contents of `~/.tauri/asmr-recorder-updater.key`) and the Apple Developer ID secrets to GitHub repo settings.
